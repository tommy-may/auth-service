NODE_ENV=development
PORT=

# Comma-separated list of allowed CORS origins.
# In development defaults to http://localhost:5173.
# In production must be set explicitly, e.g.: "https://app.example.com,https://admin.example.com"
ALLOWED_ORIGINS=

DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db-name

DUMMY_HASH=

JWT_SECRET=
